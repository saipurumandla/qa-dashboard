export class TestCase {
    $key ?: string;
    testCaseId ?: string;
    status ?: number;
    description ?: string;
    url ?: string;
    closed ?: Date;
    closedBy ?: string;
    created ?: Date;
    createdBy ?: string;
    modified ?: Date;
    modifiedBy ?: string;
}
